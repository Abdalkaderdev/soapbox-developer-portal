apiVersion: v1
kind: Secret
metadata:
  name: developer-portal-secrets
  namespace: soapbox
type: Opaque
stringData:
  # Generate with: openssl rand -base64 32
  nextauth-secret: "REPLACE_WITH_ACTUAL_SECRET"
